import { writable } from 'svelte/store';

export const connectionSecure = writable({});
export const socketStore = writable({});

export const privateSetting = writable("");
export const notificationSetting = writable("");
